# Copy this file to `.env` (same folder) and fill in your real values.
# The .env file is git-ignored so your credentials never get pushed.

# Your Postgres connection string from Neon
# (e.g. postgresql://user:pass@host/db?sslmode=require)
DATABASE_URL=

# Optional — port for the API server (defaults to 3001)
# PORT=3001
