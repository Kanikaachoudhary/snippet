# Copy this to `.env` (same folder) and fill in if needed.
# Leave empty for local dev (Vite proxies /api to the local server).
# For production builds (Vercel), set this to your deployed backend URL.
#
# Example: VITE_API_URL=https://snipp-api.onrender.com
VITE_API_URL=
